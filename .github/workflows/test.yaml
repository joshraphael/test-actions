name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@1a2b7b2af7c3463ddc79e877f43d64aa0311eb48
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
