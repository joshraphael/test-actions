name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@14be73172b4a564e1a5a17d90a89b22e1a6b6ff8
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
