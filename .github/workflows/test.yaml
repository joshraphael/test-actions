name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@f422d5db18565558bb0a85acb5359d5cac77bd83
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
