name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@82575434f9575f34167732369296ab5b1e527233
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          report: true
          severity: error
