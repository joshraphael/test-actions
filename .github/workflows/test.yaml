name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@56ab3106968b0edc761d86ca3ad9476f63699f31
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
