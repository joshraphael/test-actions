name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@9100db819485c32f32b46fa041fc0b9fcfda67fa
        with:
          game-id: 4111
          rascript: 4111
          report: true
          severity: error
