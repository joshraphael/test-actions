name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@f8444e71bc09da6799b9f96481a2b1f690a420e1
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
