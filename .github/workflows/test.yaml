name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@efaffcc33a4a500706cdeb12044f9546d999a7c4
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
