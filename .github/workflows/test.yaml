name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@0e199ee20e8c0977a96585922c0a0606f07b4304
        with:
          game-id: 4111
          rascript: 4111
          report: true
          severity: error
