name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@b9ce41c481375c554d15599fd6ce488778558590
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
