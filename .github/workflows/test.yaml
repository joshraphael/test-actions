name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@b2765099f8dcf942471cb26c68d44297f8ec5103
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
