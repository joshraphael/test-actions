name: Go

on:
  push:
    branches: [ "main" ]
  pull_request_target:
    branches: [ "**" ]

permissions:
  contents: write
  pull-requests: write
jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Test RAScript Audit
        uses: joshraphael/rascript-audit@f3db14887c983a9d20e6646ba3a7a72981155246
        with:
          game-id: 4111
          rascript: rascripts/4111.rascript
          # report: true
          # severity: error
